import React from 'react'
import styles from '../styles/Hero.module.css';

const Hero = () => {
  return (
    <div className={`${styles.hero}`}>
       <h1>hero</h1>
    </div>
  )
}

export default Hero